mutation addResult($resultInput: ResultInput!) {
  addResult(resultinput: $resultInput) {
    score
    target {
      angle
      trigFunc {
        sin
        cos
        tan
        calcsin
        calcCos
        calcTan
      }
    }
    user {
      name
    }
  }
}
